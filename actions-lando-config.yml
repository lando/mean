# Disable the "core" mean plugins
disablePlugins:
  - lando-mean

# Make sure we are dogfooding this plugin for testing purposes
plugins:
  "@lando/mean": /home/runner/work/mean/mean

# Disable metrics reporting
stats:
  - report: false
    url: https://metrics.lando.dev
